language: python

sudo: false

python:
  - "2.7"
  - "3.4"
  - "pypy"

install:
  - pip install -r dev_requirements.txt
  - python install .
  - pushd dependencies/enstaller
  - python install .
  - popd

script:
  - haas simplesat
